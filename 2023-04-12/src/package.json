{
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "slidev",
    "build": "slidev build --base /2023/unpoly/ --out ../../dist/2023/unpoly",
    "export": "slidev export --per-slide --output ../2023-04-23-unpoly.pdf",
    "play": "slidev --remote --tunnel"
  }
}
